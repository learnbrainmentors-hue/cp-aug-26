class Solution {
public:
    int climbStairs(int n) {
         if(n==0 || n==1){
            return 1;
        }
        int first = climbStairs(n-1);
        int second = climbStairs(n-2);
        return first + second;
    }
};
